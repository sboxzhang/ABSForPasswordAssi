<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context=".AddActivity" >

    <TextView
        android:id="@+id/lbl_account"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="@string/lbl_account" >
            <requestFocus />
    </TextView>

    <EditText
        android:id="@+id/txt_add_account"
        android:inputType="text"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentLeft="true"
        android:layout_below="@id/lbl_account"
        android:ems="10" >
            <requestFocus />
        </EditText>

	<TextView
        android:id="@+id/lbl_pwd"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/txt_add_account"
        android:text="@string/lbl_pwd" >
            <requestFocus />
        </TextView>
    
    <EditText
        android:id="@+id/txt_add_pwd"
        android:inputType="text"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentLeft="true"
        android:layout_below="@id/lbl_pwd"
        android:ems="10" >
            <requestFocus />
        </EditText>
    
    <TextView
        android:id="@+id/lbl_info"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/txt_add_pwd"
        android:text="@string/lbl_info" >
            <requestFocus />
        </TextView>

        <EditText
        android:id="@+id/txt_add_info"
        android:inputType="textMultiLine"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentLeft="true"
        android:layout_below="@id/lbl_info"
        android:ems="10" >
            <requestFocus />
        </EditText>

        
</RelativeLayout>